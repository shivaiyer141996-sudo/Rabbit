# Local-only defaults. Replace every credential outside local development.
POSTGRES_DB=rabbit
POSTGRES_USER=rabbit
POSTGRES_PASSWORD=rabbit_local_password

# The backend uses the local container above by default. Leave these commented
# for Milestone 5. A future PostgreSQL move overrides only these DATABASE_*
# settings; product code and Flyway migration history remain unchanged.
# DATABASE_URL=jdbc:postgresql://database-host:5432/rabbit?sslmode=require
# DATABASE_USERNAME=rabbit_runtime
# DATABASE_PASSWORD=replace-with-the-target-database-secret
DATABASE_MAX_POOL_SIZE=20
DATABASE_MIN_IDLE=2
DATABASE_CONNECTION_TIMEOUT_MS=10000
DATABASE_VALIDATION_TIMEOUT_MS=3000
DATABASE_MAX_LIFETIME_MS=1800000
DATABASE_MIGRATION_LOCATIONS=classpath:db/migration

JWT_SECRET=replace-with-at-least-48-random-characters
JWT_ACCESS_TTL_MINUTES=15
JWT_REFRESH_TTL_DAYS=7
LOGIN_MAX_FAILED_ATTEMPTS=5
LOGIN_LOCK_DURATION=PT30M
INVITATION_TTL=PT72H
INVITATION_ACTIVATION_BASE_URL=http://localhost/activate

MINIO_ROOT_USER=rabbit
MINIO_ROOT_PASSWORD=rabbit_local_minio_password
MINIO_BUCKET=question-assets

RABBITMQ_DEFAULT_USER=rabbit
RABBITMQ_DEFAULT_PASS=rabbit_local_queue_password

NEXT_PUBLIC_API_BASE_URL=/gateway/backend
BACKEND_INTERNAL_URL=http://backend:8080/api/v1
SESSION_COOKIE_SECURE=false

RABBIT_RELEASE_VERSION=1.0.0
RABBIT_ENVIRONMENT=local
RATE_LIMIT_ENABLED=true
RATE_LIMIT_ANONYMOUS_PER_MINUTE=20
RATE_LIMIT_AUTHENTICATED_PER_MINUTE=300
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost
